# Olamba - Быстрые заметки для macOS

Нативное macOS приложение на SwiftUI для быстрого ввода текста с копированием в буфер обмена и голосовой транскрибацией.

## Возможности

- 🚀 Открывается по глобальному хоткею **Cmd+Shift+`**
- 📝 Многострочное поле ввода с автофокусом
- 📋 Копирование в буфер по нажатию **Enter**
- 🎤 Голосовой ввод с транскрибацией
- 💾 История всех заметок (до 50 элементов)
- 🔍 Поиск по истории
- 🎯 Floating window поверх всех окон
- 🖥️ Иконка в menu bar
- ⚡ Автоматическое закрытие после копирования

## Установка

### Из DMG (рекомендуется)

1. Откройте `Olamba-1.0.dmg`
2. Перетащите `Olamba.app` в папку `Applications`
3. Откройте Olamba из папки Программы

### Сборка из исходников

```bash
./build.sh              # Собрать Olamba.app
open Olamba.app         # Запустить
```

### Создание DMG для распространения

```bash
./create_dmg.sh         # Создаёт Olamba-1.0.dmg
```

## Использование

### Горячие клавиши

| Клавиши | Действие |
|---------|----------|
| **Cmd+Shift+`** | Открыть/закрыть окно |
| **Enter** | Скопировать текст и закрыть |
| **Esc** | Закрыть без копирования |
| **Cmd+,** | Настройки |

### Menu Bar

Иконка 📝 в menu bar:
- Клик → открыть окно
- "Открыть Olamba" → показать окно
- "Выход" → закрыть приложение

### Голосовой ввод

1. Нажмите кнопку "Голос" или используйте хоткей
2. Говорите — текст будет транскрибирован
3. Результат появится в поле ввода

## Автозапуск при старте macOS

**Способ 1: Системные настройки**
1. Системные настройки → Основные → Объекты входа
2. Нажмите **+** и выберите **Olamba**

**Способ 2: LaunchAgent** (уже настроен)
```bash
# Проверить статус
launchctl list | grep olamba

# Отключить автозапуск
launchctl unload ~/Library/LaunchAgents/com.olamba.app.plist

# Включить автозапуск
launchctl load ~/Library/LaunchAgents/com.olamba.app.plist
```

## Требования

- macOS 13.0 или новее
- Apple Silicon (arm64) или Intel с Rosetta
- Command Line Tools (для сборки)

## Структура проекта

```
Olamba/
├── Olamba.swift           # Главный файл приложения
├── Olamba.app/            # Собранное приложение
├── Info.plist             # Метаданные приложения
├── Olamba.entitlements    # Права доступа
├── AppIcon.icns           # Иконка приложения
├── build.sh               # Скрипт сборки
├── create_dmg.sh          # Скрипт создания DMG
├── generate_icon.swift    # Генератор иконки
└── Olamba-1.0.dmg         # DMG для распространения
```

## Лицензия

MIT
